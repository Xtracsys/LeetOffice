# hello-leetoffice

Starter skill demonstrating the registry format (BUILD_SPEC §9.1).

## Procedure

1. `search` for the teammate's most recent note (`type: "doc"`, limit 1).
2. `read_doc` the top hit.
3. Summarize the note in one sentence and greet the author by their audit
   attribution (`audit.last_editor`).
