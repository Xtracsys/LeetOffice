# store-stats

Bundled example tool. Uses `audit_query` to count changes per actor over the
last 24 hours and `search` to surface the most-edited documents. Promotes to
stable automatically after 3 clean uses (threshold set for demo purposes).
